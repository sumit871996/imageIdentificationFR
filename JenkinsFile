pipeline {
     agent {
     label 'imageidentififr'
     }

    stage('Checkout') {
        echo 'inside checkout'
        git branch: 'main', url: 'https://github.com/sumit871996/imageIdentificationFR.git'
    }
    
    stage('docker build')
    {
        steps{
        echo 'hii'
        sh 'docker login'
        }
    }
   
}
